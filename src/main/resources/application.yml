spring:
  profiles:
    active:
      - dev
    group:
      local:
        - db-local
        - common-local
      dev:
        - db-dev
        - common-dev
    include:
      - db
      - common
      - oauth